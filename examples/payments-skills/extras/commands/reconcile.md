---
description: Walk through reconciling a payments batch with the ledger.
---

Reconcile the batch the user names: fetch the batch totals, compare them
with the ledger export, and list every mismatch with its amount.
